{
  "name": "@skycoin4444/recurring-revenue",
  "version": "1.1.0",
  "description": "Validated recurring-subscription metrics library using integer minor units",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "audit": "npm audit --audit-level=high",
    "verify": "npm run lint && npm test && npm run build && npm run audit"
  },
  "keywords": ["skycoin4444", "billing", "subscriptions", "mrr"],
  "author": "SKYCOIN4444",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.2.0"
  }
}
