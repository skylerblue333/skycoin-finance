{
  "name": "@skycoin4444/finance-core",
  "version": "0.1.0",
  "description": "Deterministic recurring-revenue and allocation calculations for SKYCOIN4444",
  "type": "module",
  "main": "src/finance-core.js",
  "exports": "./src/finance-core.js",
  "scripts": {
    "check": "node --check src/finance-core.js && node --check tests/finance-core.test.js",
    "test": "node --test tests/finance-core.test.js",
    "audit": "npm audit --omit=dev --audit-level=high",
    "pack:check": "npm pack --dry-run"
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": ["finance", "recurring-revenue", "allocation"],
  "license": "MIT"
}
